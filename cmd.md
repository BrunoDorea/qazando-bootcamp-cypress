# Bootcamp Cypress - QAzando

instalar o node

```cli
npm init -y
```

instalar o cypress

```cli
npm install cypress
```

iniciando o cypress

```cli
npx cypress open
```

executar testes

```cli
npx cypress run
```
